[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "kuvy"
version = "0.1.0"
dependencies = [
]

[project.scripts]
debug = "kuvy.main:main"

[tool.kuvy.run-paths]
main = "src/kuvy/main.py"
tests = "tests/tests.py"
