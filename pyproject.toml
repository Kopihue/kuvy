[build-system]
requires = [
    "hatchling",
]
build-backend = "hatchling.build"

[project]
name = "kuvy"
version = "0.2.0"
dependencies = [
    "kopilogs",
]

[project.scripts]
kuvv = "kuvy.main:main"

[tool.kuvy.run-paths]
main = "src/kuvy/main.py"
tests = "tests/tests.py"
